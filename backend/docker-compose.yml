services:
  backend:
    build: .
    ports:
      - "3001:3001"
    volumes:
      - tracker-data:/app/data
    environment:
      - NODE_ENV=production
      - PORT=3001
    restart: unless-stopped

volumes:
  tracker-data:
